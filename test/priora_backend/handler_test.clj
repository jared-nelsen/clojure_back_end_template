(ns priora-backend.handler-test
  (:require [clojure.test :refer :all]
            [ring.mock.request :as mock]
            [priora-backend.core :refer :all]))

(deftest test-app
  (testing "main route"))
